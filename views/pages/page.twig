{% extends "base.twig" %}

{% block content %}
	<article class="main-content post-type-{{ post.post_type }}" id="post-{{ post.ID }}">
		<div class="main-content-hero">
			<h1>{{ post.title }}</h1>
		</div>
		{{ post.content }}
	</article>
{% endblock %}